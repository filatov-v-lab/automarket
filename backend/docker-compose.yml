version: '3.8'

services:
  mongo:
    image: mongo:7
    container_name: automarket-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: automarket
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
